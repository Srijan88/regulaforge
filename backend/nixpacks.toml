[phases.setup]
nixPkgs = ["go", "python311", "git"]

[phases.build]
cmds = [
  "git clone https://github.com/veeainc/lobstertrap.git /tmp/lt",
  "cd /tmp/lt && go build -o /app/lobstertrap-linux .",
  "pip install -r requirements.txt"
]

[start]
cmd = "bash start.sh"
